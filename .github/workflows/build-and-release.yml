name: Build & Release

on:
  push:
    branches: ["master", "release"]
    tags: ['v*.*.*-release', 'v*.*.*-master']
    

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: sudo apt update && sudo apt install -y nvidia-cuda-toolkit libglew-dev libglfw3 libglfw3-dev

      - name: Configure
        run: |
          cd ProjectSource
          cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release

      - name: Package
        run: |
          cd build
          tar czf lbm-linux-Release-${GITHUB_REF_NAME}.tar.gz AppFacadeExec
         
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        with:
          files: build/*.tar.gz, build/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
